# راه‌اندازی دستیار هوشمند فارسی

این قابلیت در اپ `ai_assistant` قرار دارد و از سطح دسترسی همان کاربر استفاده
می‌کند. داده‌های طرح/پروژه/زیرپروژه قبل از ارسال به سرویس، از استان‌های مجاز
کاربر فیلتر می‌شوند.

## نصب روی cPanel

1. کد و migration جدید را deploy کنید.
2. در Python App یا فایل محیطی cPanel این مقادیر را تنظیم کنید (یا از فرم
تنظیمات سراسری در admin استفاده کنید):

```text
GAPGPT_API_KEY=کلید-سرویس
GAPGPT_API_URL=نشانی-واقعی-API-ارائه‌شده-برای-حساب
GAPGPT_MODEL=نام-مدل-اختیاری
TAVILY_API_KEY=کلید-اختیاری-جستجوی-وب
AI_LOCAL_JS_ENABLED=false
AI_JS_WORKSPACE_ROOT=/ مسیر/پوشه اسکریپت‌های مجاز
AI_NODE_BINARY=node
AI_JS_TIMEOUT_SECONDS=20
```

صفحه `https://gapgpt.app/platform-v2` صفحه پلتفرم است و لزوماً Endpoint API
نیست؛ بنابراین `GAPGPT_API_URL` باید از مستندات/حساب API شما مقداردهی شود.
کلید در مرورگر نمایش داده نمی‌شود.

3. دستورات زیر را اجرا کنید:

```bash
python manage.py migrate
python manage.py collectstatic --noinput
```

4. در `/admin/ai_assistant/aiuserpolicy/` برای هر کاربر می‌توان فعال‌سازی،
سقف روزانه/ماهانه، اجازه جستجوی وب، اجازه عملیات تغییردهنده و کلید اختصاصی
را تنظیم کرد.

تنظیمات endpoint/model و کلیدهای GapGPT/Tavily در
`/admin/ai_assistant/aiplatformsettings/` قرار دارد؛ کلیدها در دیتابیس رمزنگاری
و در admin به‌صورت masked نمایش داده می‌شوند. برای کنترل drift مدل و KB اجرا کنید:

```bash
python manage.py check_ai_knowledge_drift
```

## رفتار فعلی

- دکمه «دستیار هوشمند» در همه صفحات کاربران مجاز نمایش داده می‌شود و پنل
  کشویی فارسی باز می‌کند.
- پاسخ‌ها فارسی و بر اساس snapshot داده‌های قابل مشاهده کاربر هستند.
- دستیار می‌تواند درباره فیلدها توضیح فارسی، کاربرد، مثال و قابل‌ویرایش بودن
  آن‌ها را بیان کند.
- برای درخواست‌هایی مانند «نام پروژه ۱۲۳ را به ... تغییر بده»، ابتدا کارت
  پیش‌نمایش مقدار قدیم/جدید نشان داده می‌شود و فقط با کلیک کاربر روی «تأیید و
  ذخیره تغییر» به‌روزرسانی انجام می‌شود.
- فیلدهای محاسباتی و بعضی شناسه‌های حساس عمداً قابل ویرایش AI نیستند تا
  محاسبات مالی و سطح دسترسی خراب نشود.
- جستجوی وب اختیاری است و فقط برای کاربری که اجازه آن فعال شده اجرا می‌شود؛
  عبارت صریح «از روی اینترنت» نیز جستجو را خودکار فعال می‌کند.
- provider پیش‌فرض جستجو Tavily است و خروجی آن شامل خلاصه، URL، دامنه، امتیاز،
  تاریخ انتشار و نوع تقریبی منبع است. این اطلاعات برای ارجاع و مقایسه منابع
  هستند و تضمین اعتبار قطعی محسوب نمی‌شوند.
- اگر کلید Tavily تنظیم نشده باشد، جستجوی عمومی بدون کلید به‌عنوان fallback
  امتحان می‌شود؛ این fallback ممکن است با محدودیت شبکه یا نرخ درخواست cPanel
  مواجه شود.
- ابزار `run_local_js` فقط برای کاربر staff، فقط وقتی `AI_LOCAL_JS_ENABLED=true`
  باشد و فقط برای فایل `.js` داخل `AI_JS_WORKSPACE_ROOT` در دسترس است. اجرا با
  Node و بدون shell interpolation، با سقف زمان و خروجی محدود انجام می‌شود.
- ابزارهای خواندن و ممیزی مالی محاسبات deterministic انجام می‌دهند و مجوز
  سمت سرور را مستقل از prompt بررسی می‌کنند.
- هر تغییر در `AIAuditLog` ثبت می‌شود و عملیات تأییدنشده پس از ۱۰ دقیقه منقضی
  می‌شود.

## معماری عامل

هسته عامل در `ai_assistant/orchestration.py` یک حلقه ابزار محدود دارد. این
انتخاب عمداً بدون LangChain/LangGraph انجام شده تا استقرار Passenger/WSGI در
cPanel سبک، قابل پیش‌بینی و بدون worker دائمی باشد. سرویس‌دهنده پشت
`GapGPTProvider` قرار دارد و اگر API حساب GapGPT با قالب OpenAI سازگار باشد،
فراخوانی تابع (`tool_calls`) را دریافت می‌کند.

ابزارهای فعلی:

- خلاصه آماری محدوده قابل مشاهده کاربر
- جستجوی داخلی طرح، پروژه و زیرپروژه با نام یا کد
- خواندن جزئیات طرح، پروژه و زیرپروژه
- کنترل صحت تعریف پروژه و قراردادها
- ممیزی تخصیص، مبلغ نهایی قرارداد، اسناد تأییدشده، صورت‌وضعیت‌ها و پرداخت‌ها
- پیش‌بینی توضیح‌پذیر پیشرفت پروژه با SPI، CPI و تاریخ احتمالی پایان؛ محاسبه در Python/Django انجام می‌شود و به سرویس پولی یا ورود جداگانه نیاز ندارد
- توضیح فیلدهای سامانه
- جستجوی وب اختیاری با Tavily

## انتخاب ابزارهای بدون ورود و هزینه

برای این استقرار، MCP خارجی لازم نیست. دسترسی مستقیم به MySQL/MariaDB عملاً
قبلاً از طریق Django و با کنترل محدوده دسترسی انجام می‌شود؛ ایجاد یک MCP عمومی
برای دیتابیس فقط سطح خطر را بیشتر می‌کند. حافظه مکالمه نیز در
`AIConversation` و `AIMessage` ذخیره می‌شود و ابزارهای داخلی با مجوز کاربر کار
می‌کنند.

برای جستجوی وب، Tavily، Exa و Brave معمولاً به کلید سرویس نیاز دارند. گزینه
`Fetch` بدون کلید برای خواندن یک نشانی مشخص مناسب است، اما موتور جستجو نیست.
بنابراین بدون ورود یا پرداخت، انتخاب امن فعلی این است: جستجوی داخلی سامانه +
محاسبات محلی + خواندن منابع رسمیِ از قبل مشخص‌شده. فعال‌کردن هر جستجوی عمومی
بدون کلید ممکن است ناپایدار و محدودشده باشد و برای داده‌های قراردادی توصیه
نمی‌شود.

هر ابزار در سمت سرور مجوز را دوباره کنترل می‌کند. خروجی مدل هیچ‌گاه مجوز
محسوب نمی‌شود. عملیات نوشتن فقط برای فیلدهای allowlist شده، با پیش‌نمایش،
انقضای ۱۰ دقیقه‌ای، کنترل مجدد مجوز و کنترل تعارض مقدار انجام می‌شود.

مدل می‌تواند هنگام نیاز به شفاف‌سازی، گزینه‌های فارسی قابل کلیک برگرداند.
گفت‌وگوی پنل کشویی نیز شناسه مکالمه را نگه می‌دارد تا سؤال‌های بعدی در همان
زمینه پاسخ داده شوند.

## نکات عملیاتی

- نشانی `https://gapgpt.app/platform-v2` صفحه مدیریت پلتفرم است؛ نشانی دقیق
  Chat Completions را از حساب یا مستندات GapGPT بردارید و در `provider_endpoint`
  وارد کنید. در کد، نشانی تأییدنشده حدس زده نشده است.
- برای برآورد قیمت روز ایران، Tavily باید فعال باشد. پاسخ برآوردی باید تاریخ
  مبنا، منبع، فرضیات و بازه عدم قطعیت داشته باشد.
- اگر پاسخ خطای «کلید دسترسی ... معتبر نیست» یا HTTP 401 دیده شد، مشکل از
  مجوز/توکن `GAPGPT_API_KEY` است و ابزارهای وب قبل از اصلاح آن اجرا نمی‌شوند.
- اجرای کامل «هر عملیات سایت» توصیه نمی‌شود. عملیات جدید باید به‌صورت ابزار
  مشخص با validation، permission check، preview و audit اضافه شود؛ دسترسی
  مستقیم مدل به ORM یا shell عمداً وجود ندارد.
- پس از deploy، علاوه بر migrate و collectstatic، این بررسی‌ها را اجرا کنید:

```bash
python manage.py check
python manage.py check_ai_knowledge_drift
python manage.py test ai_assistant
```
