QUICK FIX: decimal.InvalidOperation Error
==========================================

This error occurs because MySQLdb has compatibility issues with MariaDB.

SOLUTION: Install PyMySQL and use it as MySQLdb replacement

STEP 1: Install PyMySQL
------------------------
cd ~/public_html/PMD/ProjectManagerDashboard
source ~/virtualenv/public_html/PMD/3.10/bin/activate
pip install PyMySQL

STEP 2: Files Already Updated
------------------------------
I've already updated:
- manage.py (added PyMySQL support)
- passenger_wsgi.py (added PyMySQL support)
- requirements.txt (added PyMySQL)

STEP 3: Test the Fix
--------------------
python manage.py migrate

If it still fails, try:
python fix_decimal_error.py

ALTERNATIVE: If PyMySQL doesn't work
------------------------------------
Try upgrading mysqlclient:
pip install --upgrade mysqlclient

Or check if .env file exists:
ls -la .env
cat .env

The error should be fixed now with PyMySQL installed!

